{
  "name": "mod-eat",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/backend/*",
    "apps/frontend/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "bun run --filter backend dev",
    "dev:buyer": "bun run --filter buyer dev",
    "dev:seller": "bun run --filter seller dev",
    "dev:all": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/web-push": "^3.6.4",
    "elysia": "^1.4.18",
    "react-dom": "^19.2.1",
    "react-router-dom": "^7.9.6",
    "web-push": "^3.6.7"
  }
}
